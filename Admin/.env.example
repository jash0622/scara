# URL of the SCARA Backend API (Prompt 1)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# For server-side route handlers that proxy to the backend
API_BASE_URL=http://localhost:4000

# Cookie name (must match middleware.ts)
# COOKIE_NAME=scara_admin_token
